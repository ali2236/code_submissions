package codeforces.c1431

fun main() {
/*    val scanner = Scanner(System.`in`);
    var t = scanner.nextInt()
    while (t-->0){
        val n = scanner.nextInt()
        val a = Array(n) {scanner.nextInt()}
        val count = hashMapOf<Long, Long>()
        for (i in a){
            count[i] = (count[i] ?: 0 + 1);
        }

        a.takeWhile { it >  }
    }*/
}